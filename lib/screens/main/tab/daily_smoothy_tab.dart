import 'package:flutter/material.dart';

class DailySmoothyTab extends StatelessWidget {
  const DailySmoothyTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(),
    );
  }
}
